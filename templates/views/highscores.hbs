<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/highscoresStyle.css">
    <link rel="stylesheet" href="/css/sidePanel.css">

    <title>High Scores</title>
</head>
<body>
    {{>navbar highscoresActive = highscores}}

    <div id="MainScoreboard">
        <h2>High Scores</h2>
        <div id="AllTheScores">
            {{#each scoreList}}
            <div class="highScoreEntry">
                <div class="HSUsername">{{this.username}}</div>
                <div class="HSScore"> {{this.score}}</div>
            </div>
            <hr>
            {{/each}}
        </div>
    </div>

    <h2>High Scores</h2>
    <ol>
        {{#each scoreList}}
        <li>{{this.username}} {{this.score}}</li>

        {{/each}}
    </ol>

    <script  src="/sidePanel.js"></script>

</body>
</html>